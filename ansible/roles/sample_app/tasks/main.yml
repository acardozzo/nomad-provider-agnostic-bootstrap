---
- name: Deploy whoami via the generic app role
  ansible.builtin.include_role:
    name: app
  vars:
    app_name: whoami
    app_image: "traefik/whoami:v1.10"
    app_host: "{{ traefik_domain }}"
    app_path_prefix: "/whoami"
    app_container_port: 80
    app_cpu: 100
    app_memory: 64
